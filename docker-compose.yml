# docker-compose.yml - Wordpress with PHP-FPM and MySQL
services:
  culturalskies-wp:
    image: erikngigi/wordpress-php8.3-fpm:dev
    container_name: culturalskies-wp
    restart: unless-stopped
    volumes:
      - /home/eric/culturalskies-data:/var/www/html
      - ./php/custom-settings.ini:/usr/local/etc/php/conf.d/custom-settings.ini:ro
      - ./wordpress/settings/php/php.ini-development:/usr/local/etc/php/php.ini:ro
      - ./wordpress/settings/php-fpm/www.conf:/usr/local/etc/php-fpm.d/www.conf:ro
      - ./wordpress/settings/php-fpm/zz-docker.conf:/usr/local/etc/php-fpm.d/zz-docker.conf:ro
    ports:
      - 9001:9000
    env_file:
      - .env1
    depends_on:
      wp-mariadb-1:
        condition: service_healthy
    networks:
      - wordpress-network

  deauraustinchicago-wp:
    image: erikngigi/wordpress-php8.3-fpm:dev
    container_name: deauraustinchicago-wp
    restart: unless-stopped
    volumes:
      - /home/eric/deauraustinchicago-data:/var/www/html
      - ./php/custom-settings.ini:/usr/local/etc/php/conf.d/custom-settings.ini:ro
      - ./wordpress/settings/php/php.ini-development:/usr/local/etc/php/php.ini:ro
      - ./wordpress/settings/php-fpm/www.conf:/usr/local/etc/php-fpm.d/www.conf:ro
      - ./wordpress/settings/php-fpm/zz-docker.conf:/usr/local/etc/php-fpm.d/zz-docker.conf:ro
    ports:
      - 9002:9000
    env_file:
      - .env2
    depends_on:
      wp-mariadb-2:
        condition: service_healthy
    networks:
      - wordpress-network

  wp-mariadb-1:
    image: mariadb:11.4
    container_name: culturalskies-db
    restart: unless-stopped
    volumes:
      - wordpress-mariadb-data-1:/var/lib/mysql
      - ./wordpress/settings/mysql/wordpress.cnf:/etc/mysql/conf.d/wordpress.cnf:ro
    ports:
      - 3307:3306
    env_file:
      - .env1
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - wordpress-network

  wp-mariadb-2:
    image: mariadb:11.4
    container_name: deauraustinchicago-db
    restart: unless-stopped
    volumes:
      - wordpress-mariadb-data-2:/var/lib/mysql
      - ./wordpress/settings/mysql/wordpress.cnf:/etc/mysql/conf.d/wordpress.cnf:ro
    ports:
      - 3308:3306
    env_file:
      - .env2
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - wordpress-network

networks:
  wordpress-network:
    name: wordpress-network
    driver: bridge

volumes:
  wordpress-mariadb-data-1:
    name: wordpress-mariadb-data-1
    driver: local
  wordpress-mariadb-data-2:
    name: wordpress-mariadb-data-2
    driver: local
